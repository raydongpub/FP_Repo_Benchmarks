*NOTE: We do not track the data files in GitHub because they are too large. We will put download instructions here for obtaining the proper input data. In the meantime, we only keep a local copy of the data files on NPS2 for running the tests.

